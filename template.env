# Instructions: Rename this file to .env and fill in the required values.
# Warning: Do not commit your .env file to version control as it contains sensitive information.

# SWAG (nginx + Let's Encrypt) settings
# PUID: User ID to run the swag container as (default: 1000)
PUID=1000
# PGID: Group ID to run the swag container as (default: 1000)
PGID=1000
# TZ: Timezone for the container (e.g., Etc/UTC, Europe/Berlin)
TZ=Etc/UTC
# URL: Your main domain name (e.g., example.com)
URL=
# SUBDOMAINS: Subdomains to use for SSL (comma-separated, e.g., www,)
SUBDOMAINS=www,
# VALIDATION: Validation method for Let's Encrypt (http or dns)
VALIDATION=http
# EMAIL: Email address for Let's Encrypt registration and renewal notices
EMAIL=

# pretalx settings
# PRETALX_FILESYSTEM_MEDIA: Path for pretalx media files inside the container
PRETALX_FILESYSTEM_MEDIA=/public/media
# PRETALX_FILESYSTEM_STATIC: Path for pretalx static files inside the container
PRETALX_FILESYSTEM_STATIC=/public/static

# Database settings
# POSTGRES_PASSWORD: Password for the PostgreSQL database user
POSTGRES_PASSWORD=
# POSTGRES_USER: Username for the PostgreSQL database
POSTGRES_USER=pretalx
# POSTGRES_DB: Database name for pretalx
POSTGRES_DB=pretalx

# Redis settings
# (no required env vars for default redis)
